## CLI Core

This package is a type-safe wrapper around [commander](https://www.npmjs.com/package/commander) and [zod](https://www.npmjs.com/package/zod) to define CLI commands with ease.
It provides a simple API to create commands, parse arguments and options, and handle command execution.
